<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>app.logorrr.dist.linux.flatpak</groupId>
        <artifactId>flatpak</artifactId>
        <version>${revision}${changelist}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>flatpak-dist</artifactId>

    <properties>
        <checksum.aarch64.filename>flatpak-package-${project.version}-aarch64.tar.gz.properties
        </checksum.aarch64.filename>
        <checksum.amd64.filename>flatpak-package-${project.version}-amd64.tar.gz.properties</checksum.amd64.filename>
    </properties>

    <name>${project.groupId}:${project.artifactId}</name>

    <build>
        <plugins>
            <plugin>
                <groupId>com.googlecode.maven-download-plugin</groupId>
                <artifactId>download-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>fetch-flatpak-checksum-aarch64</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>wget</goal>
                        </goals>
                        <configuration>
                            <url>
                                https://www.logorrr.app/downloads/${project.version}/${checksum.aarch64.filename}
                            </url>
                            <skipCache>true</skipCache>
                            <outputDirectory>${project.build.directory}/</outputDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>fetch-flatpak-checksum-amd64</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>wget</goal>
                        </goals>
                        <configuration>
                            <url>
                                https://www.logorrr.app/downloads/${project.version}/${checksum.amd64.filename}
                            </url>
                            <skipCache>true</skipCache>
                            <outputDirectory>${project.build.directory}/</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <execution>
                        <id>filter-aarch64</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/filtered-aarch64</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>src/main/resources</directory>
                                    <filtering>true</filtering>
                                    <includes>
                                        <include>archive-filter.properties</include>
                                    </includes>
                                </resource>
                            </resources>
                            <filters>
                                <filter>${project.build.directory}/${checksum.aarch64.filename}</filter>
                            </filters>
                        </configuration>
                    </execution>
                    <execution>
                        <id>filter-amd64</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/filtered</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/filtered-aarch64</directory>
                                    <filtering>true</filtering>
                                    <includes>
                                        <include>archive-filter.properties</include>
                                    </includes>
                                </resource>
                            </resources>
                            <filters>
                                <filter>${project.build.directory}/${checksum.amd64.filename}</filter>
                            </filters>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-flatpak-archive</id>
                        <phase>process-resources</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/flatpak.archive</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}/unpacked-raw</directory>
                                    <includes>
                                        <include>app.logorrr.LogoRRR.yml</include>
                                    </includes>
                                    <filtering>true</filtering>
                                </resource>
                            </resources>
                            <filters>
                                <filter>${project.build.directory}/filtered/archive-filter.properties</filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>unpack-flatpak-yml-raw</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>app.logorrr.dist.linux.flatpak</groupId>
                                    <artifactId>flatpak-yml</artifactId>
                                    <version>${project.version}</version>
                                    <type>jar</type>
                                </artifactItem>
                            </artifactItems>
                            <outputDirectory>${project.build.directory}/unpacked-raw</outputDirectory>
                            <includes>app.logorrr.LogoRRR.yml</includes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>create-flatpak-archive</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <configuration>
                            <executable>${flatpak.binary}</executable>
                            <workingDirectory>${project.build.directory}/flatpak.archive</workingDirectory>
                            <arguments>
                                <argument>--verbose</argument>
                                <argument>--force-clean</argument>
                                <argument>--user</argument>
                                <argument>--install-deps-from=flathub</argument>
                                <argument>--install</argument>
                                <argument>flatpak-target-dir</argument>
                                <argument>app.logorrr.LogoRRR.yml</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>