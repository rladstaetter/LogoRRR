{
  # entries which appear in file dialog
  "recent-files": {
    # recently opened files. on app startup it will be checked if paths still exist.
    # if yes, opens file, if not entry will be deleted
    "log-report-definitions": {}
  },
  # settings to display log entries
  "square-image-settings": {
    # width in pixels for square which symbolizes a log entry
    "width": 7
  },
  # settings for general application appearance
  "stage-settings": {
    # height of application
    "height": 600,
    # witdh of application
    "width": 1000,
    # x coordinate for upper left corner of application
    "x": 0,
    # y coordinate for upper left corner of application
    "y": 0
  }
}